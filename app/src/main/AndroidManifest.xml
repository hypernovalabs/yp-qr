<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yappy">

    <!-- ✅ Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- ✅ Actividad principal: solo responde a TRANSACTION y LAUNCHER -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">

            <!-- 💳 Solo este intent lanza la app -->
            <intent-filter>
                <action android:name="icg.actions.electronicpayment.yappy.TRANSACTION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- 🚀 Lanzador manual -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Actividad para mostrar QR -->
        <activity
            android:name=".screens.QrResultActivity"
            android:exported="false"
            android:theme="@style/Theme.Ypqr"
            android:label="QR Result" />

        <!-- ✅ Receiver silencioso para otros intents -->
        <receiver
            android:name=".receivers.TefReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="icg.actions.electronicpayment.none.INITIALIZE" />
                <action android:name="icg.actions.electronicpayment.yappy.SHOW_SETUP_SCREEN" />
                <action android:name="icg.actions.electronicpayment.yappy.FINALIZE" />
                <action android:name="icg.actions.electronicpayment.yappy.GET_VERSION" />
                <action android:name="icg.actions.electronicpayment.yappy.GET_BEHAVIOR" />
                <action android:name="icg.actions.electronicpayment.yappy.GET_CUSTOM_PARAMS" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
